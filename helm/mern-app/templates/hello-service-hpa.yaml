{{- if .Values.autoscaling.helloService.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hello-service
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: hello-service
  minReplicas: {{ .Values.autoscaling.helloService.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.helloService.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.helloService.targetCPUUtilizationPercentage }}
{{- end }}
